'use strict';

/**
 * what-makes-different service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::what-makes-different.what-makes-different');
